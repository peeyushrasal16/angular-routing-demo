# Screenshots for Assignment Submission

Place your assignment screenshots in this folder:

1. **routing-home.png** — Home route (`/home`)
2. **routing-about.png** — About route (`/about`)
3. **routing-products.png** — Products route (`/products`)
4. **routing-contact.png** — Contact route (`/contact`)
5. **deployed-app.png** — Live GitHub Pages URL in browser
6. **hosting-dashboard.png** — GitHub → Settings → Pages dashboard

Tip: Include the browser address bar in routing screenshots so the URL change is visible.
